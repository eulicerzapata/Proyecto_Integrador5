name: Pipeline de Ingesta de Datos

on:
  # Se ejecuta cuando haces push a main
  push:
    branches: [ main ]
  
  # Se ejecuta cuando haces pull request a main
  pull_request:
    branches: [ main ]
  
  # Permite ejecutarlo manualmente desde GitHub Actions
  workflow_dispatch:

jobs:
  ingesta-datos:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4
    
    - name: 🐍 Configurar Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: 📦 Instalar dependencias
      run: |
        python -m pip install --upgrade pip
        pip install -e .
    
    - name: 🔑 Configurar credenciales de Kaggle
      env:
        KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
        KAGGLE_KEY: ${{ secrets.KAGGLE_KEY }}
      run: |
        mkdir -p ~/.kaggle
        echo '{"username":"${{ secrets.KAGGLE_USERNAME }}","key":"${{ secrets.KAGGLE_KEY }}"}' > ~/.kaggle/kaggle.json
        chmod 600 ~/.kaggle/kaggle.json
    
    - name: 📊 Ejecutar ingesta de datos - Carga a SQLite
      run: |
        python src/proyecto_integrador/load_to_sqlite.py
    
    - name: 📤 Exportar a CSV
      run: |
        python src/proyecto_integrador/export_to_csv.py
    
    - name: 📋 Verificar archivos generados
      run: |
        echo "=== Archivos generados ==="
        ls -lh db/
        echo ""
        echo "=== Tamaño de la base de datos ==="
        du -h db/proyecto.db
        echo ""
        echo "=== Tamaño del CSV ==="
        du -h db/export.csv
        echo ""
        echo "=== Primeras 5 líneas del CSV ==="
        head -n 5 db/export.csv
    
    - name: 💾 Subir base de datos como artefacto
      uses: actions/upload-artifact@v4
      with:
        name: database-sqlite
        path: db/proyecto.db
        retention-days: 30
    
    - name: 💾 Subir CSV como artefacto
      uses: actions/upload-artifact@v4
      with:
        name: export-csv
        path: db/export.csv
        retention-days: 30
    
    - name: 📝 Commit de archivos generados al repositorio
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add -f db/proyecto.db db/export.csv
        git diff --staged --quiet || git commit -m "Update: Base de datos y CSV actualizados automáticamente [skip ci]"
    
    - name: 🚀 Push de cambios al repositorio
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}
    
    - name: ✅ Pipeline completado
      run: |
        echo "✅ Pipeline de ingesta ejecutado exitosamente"
        echo "📊 Base de datos: db/proyecto.db"
        echo "📁 CSV exportado: db/export.csv"
        echo "🔄 Archivos guardados en el repositorio"
